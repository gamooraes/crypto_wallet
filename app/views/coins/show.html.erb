<p style="color: green"><%= notice %></p>

<%= render @coin %>

<div>
  <%= link_to translated_navigation_links(:edit), edit_coin_path(@coin) %> |
  <%= link_to translated_navigation_links(:back_coins), coins_path %>

  <%= button_to translated_navigation_links(:delete), @coin, method: :delete %>
</div>
